import React from "react";

const Use = ({ name, commonsToken }) => {

  // TODO
  // - graph (supply, price [sell, buy])
  // - buy
  // - sell
  // - balance of user
  return (
    <div>
      {name}
    </div>
  );
}

export default Use;
